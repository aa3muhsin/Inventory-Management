@page
@model Inventory_Management.Pages.StaffModel

<div class="row">
    <div class="col"><h3>Staff</h3></div>
    <div class="d-flex col justify-content-end">
        <p>
            <a asp-page="CreateStaff" class="btn btn-primary">Create New</a>
        </p>
    </div>
</div>

@foreach (var item in Model.Staff) {
        <div class="row mb-3">
            <div class="d-flex col">
                @Html.DisplayFor(modelItem => item.StaffNumber)
            </div>
            <div class="d-flex col">
                @Html.DisplayFor(modelItem => item.NationalIdentificationNumber)
            </div>
            <div class="d-flex col">
                @Html.DisplayFor(modelItem => item.Name)
            </div>
            <div class="d-flex col">
                @Html.DisplayFor(modelItem => item.Department.Name)
            </div>
            <div class="d-flex col">
                <a class="btn btn-primary" asp-page="./EditStaff" asp-route-id="@item.Id">Edit</a> |
                <a class="btn btn-primary" asp-page="./StaffDetails" asp-route-id="@item.Id">Details</a> |
                <a class="btn btn-primary" asp-page="./DeleteStaff" asp-route-id="@item.Id">Delete</a>
            </div>
        </div>
}